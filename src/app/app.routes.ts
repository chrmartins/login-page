import { Routes } from '@angular/router';
import { LoginComponent } from './pages/logig/login.component'; // Import the LoginComponent from the correct file

export const routes: Routes = [
  {
    path: "login",
    component: LoginComponent,
  }
];
